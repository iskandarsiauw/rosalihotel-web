.claude/worktrees/
.claude/settings.local.json
uploads/sess_*
uploads/media/images/*
!uploads/media/images/index.php
uploads/media/videos/*
!uploads/media/videos/index.php
uploads/splats/*
!uploads/splats/index.php

# Production DB credentials — never commit, upload manually via File Manager
includes/db.php

# Apache config — differs between local (XAMPP) and prod (cPanel/LiteSpeed)
# Each environment edits its own .htaccess directly.
# See .htaccess.example for the canonical directives the site needs.
.htaccess
# Exception: admin/.htaccess is the per-deploy admin kill switch and IS tracked.
!admin/.htaccess
